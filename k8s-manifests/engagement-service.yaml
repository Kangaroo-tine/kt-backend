# Engagement Service
apiVersion: v1
kind: Service
metadata:
  name: engagement-service
  namespace: kangaroo-tine
spec:
  selector:
    app: engagement-service
  ports:
    - port: 8083
      targetPort: 8083
  type: ClusterIP

---
# Engagement Service Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: engagement-service
  namespace: kangaroo-tine
spec:
  replicas: 1
  selector:
    matchLabels:
      app: engagement-service
  template:
    metadata:
      labels:
        app: engagement-service
    spec:
      containers:
      - name: engagement-service
        image: kangaroo-tine/engagement-service:latest
        ports:
        - containerPort: 8083
        env:
        - name: DB_USERNAME
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: username
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: password
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: jwt-secret
              key: secret
        - name: SPRING_PROFILES_ACTIVE
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: SPRING_PROFILES_ACTIVE
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "250m"
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8083
          initialDelaySeconds: 60
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8083
          initialDelaySeconds: 30
          periodSeconds: 10
